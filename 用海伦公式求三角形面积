#include<stdio.h>
#include<math.h> 
int main()
{
	int a,b,c;
	double p,sum,area;
  
	scanf("%d %d %d",&a,&b,&c);
	p = (a+b+c)*0.5;
	sum = p*(p-a)*(p-b)*(p-c);
	area = sqrt(sum);
	printf("%3f\n",area);
	
	return 0;
}
